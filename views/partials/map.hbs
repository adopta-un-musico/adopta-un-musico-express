<div id="map"></div>
<script>
  function initMap() {
    const lat = 41.3977381;
    const lng = 2.190471916;
    const center = { lat: lat, lng: lng };
    const map = new google.maps.Map(document.getElementById('map'), {
      zoom: 9,
      center: center,
      scrollwheel: false
    });
    // la siguiente variable debereía mostrar la información del evento en el pin. 
    // No lo hace, investigar bien
    const contentString = `
        <strong>{{{event.name}}}<br />
        {{{event.location}}}</strong>
      `
    const infowindow = new google.maps.InfoWindow({
      content: contentString
    });
    const marker = new google.maps.Marker({
      position: center,
      map: map
    });
    marker.addListener('click', () => {
      infowindow.open(map, marker);
    });
  }
</script>
<script async defer
  src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAi9ges1EKuaHPXXffNTJ4dAsQH3GYygEo&callback=initMap"></script>